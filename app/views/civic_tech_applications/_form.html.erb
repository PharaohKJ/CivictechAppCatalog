<%= form_with(model: civic_tech_application, local: true) do |form| %>
  <% if civic_tech_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(civic_tech_application.errors.count, "error") %> prohibited this civic_tech_application from being saved:</h2>

      <ul>
      <% civic_tech_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
